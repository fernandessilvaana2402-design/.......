from flask import Flask

app = Flask ( __name__ )

app . route('/')
def home() :
    return "h1 servidor python ativo!</h1><p> 0 back-end funcionou no Replit . </p>"
if __name__ == '__main__' :
    app . run ( host='0.0.0.0' , port=8080 )
